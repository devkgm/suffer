import { StyleSheet, View } from 'react-native';

export default Order = ({ product, quantity, price }) => {
    return <View style={styles.container}></View>;
};

const styles = StyleSheet.create({
    container: {},
});
